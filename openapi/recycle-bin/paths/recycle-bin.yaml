get:
  summary: Get records
  tags:
    - Recycle Bin
  parameters:
    - $ref: '../../openapi.yaml#/components/parameters/perPageParam'
    - $ref: '../../openapi.yaml#/components/parameters/pageParam'
    - $ref: '../../openapi.yaml#/components/parameters/includeParam'
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../responses/RecycleBinCollection.yaml'
  operationId: get-recycle-bin
  description: Returns a paginated list of all recycle bin items.
